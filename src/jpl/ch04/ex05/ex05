(a) N分木のノードを表すTreeNode
    インターフェイス
    　各ノードで表示方法が異なるのであれば多重継承が必要になる
(b) 特定の順番で木を探索するTreeWalker
    抽象クラス
    　深さを図るGetDepthのようなものは共通だが，探索方法が変わる
(c) グラフィックシステムにより描画可能なオブジェクトのためのDrawable
    　抽象クラス
    　座標とかの実装や色などのフィールドとかは共通だが，太さや色を表現するときのふるまいが変わってきそう
(d) グラフィックデスクトップから実行できるプログラムのためのApplication
    具象クラス
    　デスクトップから実行で着るのであれば環境によって変化するふるまいはない